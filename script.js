window.onload = function inicialColor() {
  const selectedColor = document.createElement('class');
  selectedColor.className = 'selected';
  const blackColor = document.querySelector('.color');
  blackColor.classList.add("selected");
};
